<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pmc">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PMC">
        <activity android:name="com.example.memo.ViewNote">
        </activity>
        <activity android:name="com.example.pmc.WriteNote"></activity>
        <activity android:name=".SetActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

    <uses-permission android:name="android.permission.VIBRATE"/>    //진동 권한 얻기 - 권한 얻을시 어플 밖에
</manifest>

    <!--        <activity android:name=".MainActivity2"></activity>-->